#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a and @b: are two paramater to add them
 * Return: the value som of @a and @b
 */
int add(int a, int b)
{
	return (a + b);
}
